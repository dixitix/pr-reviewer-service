version: "2"

run:
  tests: true

linters:
  default: standard
  enable:
    - errcheck
    - staticcheck
    - ineffassign
    - revive

formatters:
  enable:
    - gofumpt

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0

output:
  format: colored-line-number
